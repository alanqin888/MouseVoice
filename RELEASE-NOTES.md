# MouseVoice 0.3 测试版

原生 macOS 菜单栏小工具：原地长按鼠标左键约 0.5 秒，发送 Fn 按下；松开发送 Fn 释放。移动超过容差则取消。触控板物理按下使用相同左键事件路径。

不联网、不录音、无第三方运行依赖。语音处理由用户当前 Fn 绑定的系统功能或输入法负责。

本版本为 Apple Silicon 测试构建，需输入监控与辅助功能/设备控制权限。已验证全局合成鼠标到 Fn 事件链路；物理设备及不同输入法兼容性仍需实际验收。未进行 Apple 分发签名或公证。

网站素材：`Resources/MouseVoice-icon-1024.png`。应用包：`MouseVoice.app.zip`。源码与构建脚本均在本目录。

0.3：改用黑白鼠标/声波菜单栏图标；精简菜单并提供 0.3/0.5/0.8/1.2 秒快速切换；阻止重复运行。构建只生成压缩包，避免多个应用图标。

启动修正：记住上次明确选择的启用/暂停状态；重开应用后恢复，首次运行仍需授予权限并手动启用。macOS 对本地临时签名的更新可能要求重新关联输入监控和辅助功能授权。
