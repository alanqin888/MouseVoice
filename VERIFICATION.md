# MouseVoice 0.2 验证记录

## 0.4 恢复状态测试

2026-09-22：`bash test-session.sh` 六项无输入注入测试通过，覆盖重叠的休眠、锁屏、会话中断，锁屏时唤醒不启用，全部恢复后仅启用一次，重复通知不会关闭，手动暂停不会恢复。`test.sh` 30 项断言通过，构建和严格签名检查通过。未通过实际锁屏/睡眠操作完成端到端验收。

2026-09-21，macOS 27.0 / Apple Silicon。

## 已验证

- 30 项状态机及键盘事件结构断言通过。
- 真实 Quartz 全局事件测试通过：短按不触发，拖动后返回不触发，原地长按约 1.2 秒产生 Fn down，松开产生 Fn up，触发后拖动释放。
- 使用生产 AppDelegate、HoldDetector、KeyOutput，输入由测试程序合成，不冒充物理鼠标/触控板测试。结果见 `Tests/system-events-result.txt`。
- Fn 在 HID head 监听位置可观察；session 位置未观察到同样的 Fn，而 Shift 对照可观察。只能确认 Fn 在中间被处理，不能判断具体处理者。
- 原创 PNG 图标与多尺寸 ICNS 已包含在 0.2 应用中。
- 应用安装在 `/Applications/MouseVoice.app`，严格签名检查通过。

## 安装副本实测

20:58 已安装副本通过外部进程合成鼠标测试：约 1.2 秒后发送 Fn down，并在 HID 监听回读；鼠标松开发送 Fn up 并回读；拖动取消有日志。此前切换输入监控开关并重启仍失败；只重置本应用 ListenEvent 旧授权记录并重启后恢复。该结果支持旧授权状态异常是原因，但不证明 macOS 内部具体缓存机制。

回归验收规则：必须检查 Finder/LaunchServices 启动的安装副本；不能仅以设置开关、CLI 权限或“监听已启用”日志作为成功依据。当前输入来自外部测试程序，物理设备和实际语音启动仍待用户确认。

按用户要求，不绑定或识别任何具体输入法，只发送 Fn。未证明所有输入法都接受合成 Fn，也未验证物理触控板操作。

## 0.3 菜单和副本整理

2026-09-22：用户确认物理鼠标长按已能启动当前语音输入。随后按下到 Fn 的独立回读实测为 0.803 秒（0.8 配置）和 0.509 秒（0.5 配置）。

0.3 版 30 项逻辑断言通过，构建压缩包完成并通过严格签名检查；源码改为单色模板菜单图标、精简日常菜单、内置长按时间选择与防重复实例。构建脚本只生成压缩包。两份历史构建展开副本已移至废纸篓；Spotlight 仅剩 `/Applications/MouseVoice.app`。

2026-09-22 启动修正回归：此前安装版因本地签名更新而失去发送权限，正常启动又默认暂停。重新关联当前安装版的授权，并重置本应用旧输入监控记录后，不带 `--enable` 的普通启动日志确认“监听已启用；原地长按左键 0.5 秒”。外部合成鼠标进程确认开始事件、释放事件与拖动取消；`enabledOnLaunch` 偏好值为 1。此项不等同于物理触控板或当前语音软件的端到端验收。

拖拽修正待安装版回归：语音活跃时，事件监听改为拦截该阶段的 `leftMouseDragged`，并保留 `leftMouseUp` 用于释放 Fn。临时测试进程因它不是获授权的安装应用，不能创建可拦截的输入监听；基础 30 项逻辑测试通过，安装版授权刷新后需做物理鼠标验收。
